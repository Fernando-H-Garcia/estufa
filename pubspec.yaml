name: estufa
description: Controle para estufa
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  crc: ^0.0.2
  scoped_model: ^2.0.0
  cupertino_icons: ^1.0.5
  masked_text: ^1.1.2
  wakelock: ^0.6.2
  flutter_bluetooth_serial: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1

flutter:

  uses-material-design: true

